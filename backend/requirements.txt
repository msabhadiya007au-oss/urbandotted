# Urban Dotted Expense Book — production backend requirements
# Cleaned for external hosting (Render, Fly, Railway, etc.) on Python 3.12.
# No Emergent-internal packages.

# --- Web framework ---------------------------------------------------------
fastapi==0.110.1
starlette==0.37.2
uvicorn[standard]==0.30.6

# --- Data validation -------------------------------------------------------
pydantic==2.9.2
pydantic-core==2.23.4
email-validator==2.2.0
annotated-types==0.7.0
typing_extensions==4.12.2

# --- MongoDB ---------------------------------------------------------------
motor==3.5.1
pymongo==4.8.0
dnspython==2.6.1

# --- Auth ------------------------------------------------------------------
bcrypt==4.2.0
PyJWT==2.9.0

# --- Config / env ----------------------------------------------------------
python-dotenv==1.0.1

# --- Crypto (bank details encryption via Fernet AES-128-CBC + HMAC) --------
cryptography==43.0.1

# --- HTTP client (used by auth session exchange + legacy storage) ---------
requests==2.32.3
certifi>=2024.7.4
urllib3>=2.2.2

# --- File uploads (FastAPI form/multipart) ---------------------------------
python-multipart==0.0.9

# --- PDF & report generation ----------------------------------------------
reportlab==4.2.5
pillow==10.4.0

# --- Object storage (S3-compatible: AWS, R2, B2, MinIO, DO Spaces) --------
boto3==1.35.36
botocore==1.35.36

# --- Tests -----------------------------------------------------------------
pytest==8.3.3
